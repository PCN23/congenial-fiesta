// import functions and grab DOM elements
const climateDiv = document.getElementById('Climate button');
const architectureDiv = document.getElementById('Architecture button');
// let state
let Climate = 0;
let Architecture = 0;
// set event listeners 
climateDiv.addEventListener('change', () => {
  
})
  // get user input
  // use user input to update state 
  // update DOM to reflect the new state
